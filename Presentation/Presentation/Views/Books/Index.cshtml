@model IEnumerable<Service.Models.Book>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>BOOKS</h2>

@if (!Model.Any())
{
    <p>There are no books</p>
}

<div class="search-div">
    @using (Html.BeginForm())
    {
        @Html.TextBox("searchBox")
        <a href="#" class="btn searchBtn">Search</a>
        @*<input type="submit" value="search"/>*@

    }
</div>

<div class="list-group">
    <table style="width:100%;">
        <tr class="list-header">
            <td class="list-column list-column-first">TITLE</td>
            <td class="list-column list-column-second" >AUTHORS</td>

        </tr>
        @foreach (var book in Model)
        {
            <tr onclick="location.href='@Url.Action("Info", "Books", new { id = book.ISBN }, null)'" class="list-item">
                <td class="list-column list-column-first">
                    @book.Title
                </td>
                <td class="list-column list-column-second">
                    @book.ISBN
                </td>

            </tr>
        }

    </table>

</div>
    @*<ul>
        <li style="list-style: none; width: 95%;"><span class="list-column">Title</span><span class="list-column">Author</span><span class="list-column">ISBN</span></li>
        @foreach (var book in Model)
        {
            <li class="list-item"  ><span class="list-column">@book.Title</span><span class="list-column">@book.pages</span><span class="list-column">@book.ISBN</span> </li>


            @*<li class="list-item">@Html.ActionLink(book.Title, "Info", "Books", new { id = book.ISBN }, null)</li>*@

    }
 

    @Html.ActionLink("Create Book", "Add", "Books", new { @class = "btn createBtn" })
